# 第1—4课教师材料修订记录

日期：2026-09-19。基于 `main` 提交 `f4ec8a2705a11b15b50fcc410e3b99a445b431e4` 审核。范围仅为 `instructor-guide/lesson-01` 至 `lesson-04`，不改学生模板、教学数据、评分规则、已发布自查题库或运行服务。

## 审核依据与修订

对照[课程标准](../COURSE_DESIGN_STANDARD.md)、[课程总表](../COURSE_MAP.md)、[术语规范](../TERMINOLOGY_AND_FOUNDATIONS.md)、[评分规则](../GRADING.md)、第1—4课学生任务及服务数据说明修订。保留全程使用AI、独立当堂成果、前75分钟包含学习与核对、末15分钟自查等安排，不以禁止AI或新增答辩降低任务开放性。

| 课次 | 已发现的问题或需要补强之处 | 本次处理 |
|---|---|---|
| L01 | 教师指导缺少可直接讲解的完整例子；旧工作区路径不适合当前合并仓库 | 补充工单/人数、等待终点、分母和三层结论示范；默认定位 `student-template`，保留显式路径及历史目录兼容 |
| L02 | 教师合格要求只写一项对照，与学生两到三种替代口径不一致；空组均值相减会出错 | 按现行学生任务统一要求；示范目标变化与算法错误的区别；未定义差值输出 `null` |
| L03 | 参考解析不能完整处理空白、非数值和非有限等待；未知放弃状态未获区分；审计缺少旧版本行数分解 | 增加明确标记，按用途保留记录；核对“原行＝工单＋完全重复行＋被替代行”；保留全部待核编号及原因，增加窗口归属数量 |
| L04 | 比例零分母及并列处理不明确；指标定义和分母需要更直接的核对入口 | 输出分子、分母及严格超时定义；复合指标不重复计数；精确并列同组，未定义指标不参加排序；保留原排序字段供兼容显示 |

四课均补充具体完成要求及较高质量成果的实质差异；L01—02明确只作形成性反馈、不计分，L03—04仍按现行规则计分。未新增评分上限、图表数量、必须采用的方法或学生日志要求。

## 可重复的核验命令

在完整仓库根目录运行，Python 3.10或以上、仅标准库：

```bash
python -m unittest discover -s instructor-guide/lesson-01 -p test_reference.py -v
python instructor-guide/lesson-01/reference.py
python instructor-guide/lesson-02/reference.py
python instructor-guide/lesson-03/reference.py
python instructor-guide/lesson-04/reference.py
```

也可在 `instructor-guide/` 目录执行相应 `python lesson-01/reference.py` 等命令。独立学生仓库使用 `--student-root /path/to/student-repository`。显式指定的路径无效时应报错，不改用其他数据；L03只要求本课所需的原始导出及字典，不再额外依赖第一期 `tickets.csv`。

## 本轮实际检查结果

使用仓库原生成脚本重建隔离测试数据，并按仓库换行格式逐一核对七个服务CSV的Git blob SHA，均与上述提交一致；未对远端或学生数据作任何改动。

- **34项标准库测试通过。** 覆盖四组课堂例子的数值、分位数与标准差口径、空组、零分母、字典重复键、缺失编码优先于单位换算、版本冲突、原始行数分解、未知放弃状态、严格超时阈值、复合指标不重复计数及并列。
- **四个参考入口均成功运行。** 测试分别从仓库根、教师目录及显式指定学生路径运行，JSON输出一致；执行前后服务CSV的SHA-256未变。
- **原有固定数据数值保持不变。** 修订前后四课原有数值字段逐项一致，增加的是说明、分子、审计计数和未定义/并列信息，而不是更换统计目标或改写参考结果。原有文档中的关键数字由回归测试核对。
- **新增链接检查。** 链接指向已核对的现行学生任务、课程规范、评分规则，或本轮新增及修改的教师文件，不指向历史归档。

## 尚未由本轮验证的事项

本轮没有进行真实学生90分钟试教，没有运行整个自查服务、学生提交CI或部署验收。基础阅读时间、独立完成过程及跨课迁移仍需真实课堂观察，不能由参考程序运行或AI生成成果证明。

自查题内容和ID保持原样；本轮没有把题库修改或新题上线列为完成事项。课堂中的额外边界例子只用于讲解和程序测试，不增加学生必须提交的材料，也不表示当前CSV包含所有这些异常。
