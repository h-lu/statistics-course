# 第16课参考分析与评价标准 · 根据实验结果决定推广、试用或停止

## 运行参考分析

从教师仓库运行（可显式传入学生仓库绝对路径）：

```bash
python3 lesson-16/reference.py /path/to/course-student-template
```

本工作区省略路径时会寻找相邻`lesson-01-first-green`；其他目录应显式传路径。依赖NumPy、SciPy；共同数值辅助在`lesson-09/reference_tools.py`。完整输出见[reference_result.json](reference_result.json)，代码固定模拟种子，JSON中不保存机器路径。

## 实际核算与关键关系

分配各240人；观测tool225、usual217，实际使用tool203、usual16。团队变化量差约−5.8405分钟，Welch区间[−8.8534,−2.8276]；它基于可观察变化，不能不加假设称全体ITT。缺失者两组都假定变化−5/0/+5时插补差约−5.367/−5.534/−5.701；高质量还应允许两组缺失不同。

这些数值来自所附教学数据与代码，不是现实效果。Monte Carlo数值允许随实现有小幅模拟误差；重新实现可采用不同合理算法、样本规模或区间，但需说明精度与假设。

## 可采用的分析方法

可以分析随访值并调整基线、变化量或团队汇总，条件是估计目标明确、推断保留实际随机分配单位。缺失假设和政策门槛需要有依据；可以不推广，但不能只说p大或小。

可让AI实施团队汇总、回归调整、聚类稳健区间或区组随机化检验；核对小团队数与缺失假设。无需手推检验分布。

参考代码展示一种可复核的分析方法，不是唯一解法；学生需完成的内容以本课README的分析任务为准。特别关注：报告全部随机化人数、实际有随访结果的人数、独立团队数；不将使用后分组当随机分组。效果符号明确：tool−usual为负表示处理时间下降。

## 评价重点与分析局限

- 主要结论应有可复核的数值结果和分析程序支持；仅运行示例代码不算完成本课分析。
- 至少一项与行动有关的替代分析方案或假设变化有实质结果，不能只列方法名称。
- 目标量、独立单位、单位和分母说明一致。
- 缺失和污染不会因随机化而自动消失；观察到的使用者效果不等于提供工具这一措施的效应。按原分配机制重新分配标签的随机化检验在存在处理影响缺失时需要额外假设，不能当作无条件精确证据。
- 按需查阅依从者平均因果效应、工具变量、多重插补和小样本聚类校正。采用这些方法必须列出新增假设，不能因方法名高级就更确定。

合格报告应完成分析任务并解释重要选择；高质量报告还应说明哪些假设无法由现有数据检验、比较不同假设下的结果，并提出可实施的后续方案。AI完成代码和文字完全允许，不评价作者输入比例。

## 审核状态

生成数据、学生入口和本参考已作作者/AI试跑；参考输出可重新计算。题库覆盖本课五概念及其中复习目标。没有把作者试跑称作真实学生课堂90分钟完成验证。
