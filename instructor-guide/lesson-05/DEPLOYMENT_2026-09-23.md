# 第5课作业与知识自查部署记录（2026-09-23）

第5课学生作业此前已发布于 `statistics/course-student-release-2026`，发布仓库提交 `6cc0699` 包含现行的 `lesson-05`、告警数据和同步工具。2026-09-23从该仓库重新克隆，运行 `python3 scripts/course.py start 05` 与 `run 05`，得到42天、3,780条设备日记录的起始概览。另从第5课发布前的提交模拟学生执行 `sync`，成功补入第5课和所需数据，原第4课README字节未变。现有学生私有仓库须由学生按发布仓库README手动同步；本次没有批量改写私有仓库。

知识自查新增 `v2-l05-r4`，只澄清术语和情境损失表述。GitHub课程仓库提交为 `3b0e4b5`，Gitea独立 `statistics/stat-check` 仓库提交为 `5b09ca0`。原r3题库及历史作答保持不变。完整本地服务与教师脚本测试为276项通过；独立stat-check仓库249项通过、10项因不存在教师题库镜像而跳过。

生产服务位于 `hblu.top:/home/ubuntu/stat-check-live`，数据库为该目录 `data/stat-check.sqlite3`。上线前使用SQLite在线备份接口保存到 `data/backups/stat-check-pre-l05-r4-20260923-021239Z.sqlite3`，备份完整性检查为 `ok`；备份时有64名用户、4个场次、1,504条答题记录。部署前当前场次为第4课r3的B阶段，截止于2026-09-21 06:31:46 UTC，已过期；没有第5课场次。本次未关闭或新建场次。

复制新版题库到部署目录后，重新构建并启动 `statistics-stat-check` 容器。部署后容器健康，`https://hblu.top/stat-check/healthz` 返回200；服务选择第5课r4作为新场次题库，仍能读取r3。新版文件SHA-256为 `d1c134ae2841b232bd5c440d4a8fed35d75ca9a65e87cce2526bee54c1ae9406`，与两处源文件一致。数据库快速检查仍为 `ok`，用户、场次和答题记录数不变，当前第4课场次也未变。

教师要开展第5课自查时，在教师页新建第5课场次，然后按课堂进度开放各阶段；发布题库本身不会自动开启答题。Gitea网页对未登录请求跳转到登录页，本次通过授权的SSH克隆验证发布仓库和同步工具，未把匿名浏览当作学生登录验收。
