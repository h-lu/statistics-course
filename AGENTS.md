# 统计学课程编写工作区

本规则只用于统计学课程、学生模板、教师材料及知识自查；不改写相邻的机器学习课程或设备用途。

开始课程设计、改写、审核前，完整阅读：
1. `instructor-guide/COURSE_DESIGN_STANDARD.md`：唯一现行设计标准；
2. `instructor-guide/COURSE_MAP.md`：32课知识与项目映射；
3. `instructor-guide/TERMINOLOGY_AND_FOUNDATIONS.md` 与相关课次材料；
4. `instructor-guide/STUDENT_MATERIALS_STANDARD.md`：面向初学者的学生材料编写规范。编写、改写或审核学生任务、知识准备、报告提示和示例说明时必须遵循。

默认学生全程使用AI，包括编程、分析、写作和提交。每课主任务本身必须有超出传统本科练习的实质要求；不得把难度全部移到选做，也不得以AI出错、不能一次完成或无法代做为设计依据。

规范正文在教师仓库；学生仓库只放面向学生的任务、知识、数据和工具，不混入教师编写说明、评分参考答案或题库。`AGENTS.md`不是跨独立仓库自动生效的全局规则，从其他目录启动任务时先显式读取此处规范。

学生材料使用统计学社区通行术语，并在首次出现时用完整中文和具体例子解释。不自创名称，不用未经解释的专业简称代替教学；语言简化不得改变统计含义或项目要求。具体审校步骤以学生材料编写规范为准。

编辑后核对：项目—知识—自查—评价一致；每课新增能力明确；运行可复现；数据为合成或明确授权；不依赖强制答辩、提示词记录或教师高频手动切换。按规范的六问提供实质审核证据，不仅检查文件存在。

`instructor-guide/archive/`和各仓库`archive/v1-2026-09-05`标签均为历史，只用于追溯，不作为现行模板。修改现行规范需记录原因与受影响课次。保留学生仓库、账号、历史答题记录；题目内容变化使用新的题库ID。发布前检查远端变更，禁止强推覆盖。

## 学生逐课 SUPPORT 设计（现行约定）

每课学生目录可有一份 `SUPPORT.md`，放在同课 `README.md` 与 `LEARN.md` 旁。它只面向学生，不写教师评分答案、参考结论或题库内容。它要解决的是“学生愿意完成任务，但不知道下一步是什么、为什么做、怎样判断做对”，而不是把项目改成照抄答案的练习。

### 设计目标与教学顺序

支持材料采用下面的学习链条：**具体情境 → 标准统计术语 → 小例子或手算 → 程序操作 → 结果解释 → 回到项目 → 反思和迁移**。每个核心概念至少要回答三个问题：

- 它在本课中具体指什么？
- 为什么改变这个概念会改变计算或结论？
- 学生如何从自己的数据或输出中检查它？

不能只列名词、函数和命令。首次出现的术语使用“规范中文名称＋通俗解释＋本课例子”；英文和缩写只在字段名、程序输出或查阅资料确实需要时出现。公式紧邻说明符号、分母、单位和适用条件。

### 每份 SUPPORT 的固定结构

1. **先知道今天要交出什么**：用可检查的文件和结果描述最小出口；
2. **核心概念与原理**：解释本课必要的标准统计术语，说明它们为何影响选择和结论；
3. **入门跟做**：从一句问题和运行前预计开始，给出仓库根目录、完整命令、要打开的结果文件、至少一次手算或记录核对，以及最小报告路径；
4. **入门反思**：每个主要步骤都提出一个短问题，检查分析单位、分母、单位、证据范围和下一步行动；
5. **标准完成**：实际比较一个合理的替代口径，补齐数据范围、缺失处理、局限和使用建议；
6. **提高与拓展**：提供方法、稳健性、复现、决策等方向，不以代码行数或模型数量作为提高标准；
7. **卡住时按顺序排查**：覆盖目录、命令、依赖、旧结果、数字不一致和报告无从下手等常见阻塞；
8. **提交前自检**：核对原始数据未覆盖、结果可复现、`submission.json` 路径与状态、`check` 命令和暂存内容。

### 入门跟做的步骤写法

入门路线的每一个步骤必须同时写清四件事：

1. **动作**：学生现在打开哪个文件、运行哪条命令或计算哪一个小例子；
2. **目的**：这一步要认识哪个概念，或为哪个统计判断准备证据；
3. **成功信号**：看到什么文件、字段、数值或可复算关系，才算这一步完成；
4. **反思问题**：用一句话检查分母、单位、记录范围、解释边界或下一步行动。

先让学生核对一条记录或一个小组，再扩展到完整数据；先让学生写运行前预计，再比较运行结果。程序输出是证据，不是解释本身；支持材料必须引导学生把输出翻译成统计语言。

### 三层任务的边界

- **入门跟做**降低操作和阅读门槛，但保留本课主任务的统计判断；不能替学生填写最终结论。
- **标准完成**是本课默认目标，必须完成 README 中的实质分析要求，而不是只运行示例。
- **提高与拓展**在完成主任务后进行，优先选择有理由的方法比较、敏感性分析、复现检查或面向不同使用者的决策说明；不通过堆图、堆模型或增加无关文件制造难度。

### 发布前审校

扩展或修改 SUPPORT 前，逐项核对：

- README 的问题、LEARN 的概念、SUPPORT 的步骤和知识自查使用同一术语与统计含义；
- 每个入门步骤的命令能从仓库根目录运行，结果路径和字段确实存在；
- 示例数值仅用于核对操作，并明确不能代替学生自己的运行结果；
- 反思问题能促使学生检查证据和适用范围，不要求猜教师偏好的答案；
- 标准和提高任务没有把 README 的必做要求偷偷改成选做；
- 原始数据、学生提交、历史自查记录和已发布课次不会被覆盖或删除。

先完善当前发布范围的课次，再按同一设计扩展后续课次；每次扩展记录受影响课次并重新检查链接、命令和提交路径。

## 冷读审校流程（第3课重写后采用）

重写学生材料前，先暂时不读教师讲解或参考答案，只按学生实际顺序阅读同一课的 `README.md`、`SUPPORT.md`、`LEARN.md`。把以下问题记录下来：

- 学生在前五分钟是否知道第一个具体动作；
- 第一次出现的术语是否能用本课小例子解释；
- 命令应在哪个目录运行，成功后会生成什么；
- 学生能否从一个小例子过渡到正式数据；
- 每个计算是否说明观测单位、分子、分母、单位和适用范围；
- 学生是否知道如何判断自己做对，以及出错后先检查什么。

重写时按“先判断一个小问题 → 运行起点 → 认识字段 → 选择用途 → 处理记录 → 比较方案 → 写建议”的顺序组织。`README.md` 讲任务和交付物，`SUPPORT.md` 讲陪做步骤与反思，`LEARN.md` 讲概念、原理和手算例子；三份文件不能互相重复，也不能出现互相矛盾的命令、字段或数字。

冷读结束后必须重新检查链接、命令、示例计算和 `git diff --check`。冷读是语言和路径审校，不等同于真实学生试教；两者都未做时，不得宣称材料已经验证易懂。
