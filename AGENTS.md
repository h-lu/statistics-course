# 统计学课程编写工作区

本规则只用于统计学课程、学生模板、教师材料及知识自查；不改写相邻的机器学习课程或设备用途。

开始课程设计、改写、审核前，完整阅读：
1. `instructor-guide/COURSE_DESIGN_STANDARD.md`：唯一现行设计标准；
2. `instructor-guide/COURSE_MAP.md`：32课知识与项目映射；
3. `instructor-guide/TERMINOLOGY_AND_FOUNDATIONS.md` 与相关课次材料；
4. `instructor-guide/STUDENT_MATERIALS_STANDARD.md`：面向初学者的学生材料编写规范。编写、改写或审核学生任务、知识准备、报告提示和示例说明时必须遵循。

默认学生全程使用AI，包括编程、分析、写作和提交。每课主任务本身必须有超出传统本科练习的实质要求；不得把难度全部移到选做，也不得以AI出错、不能一次完成或无法代做为设计依据。

规范正文在教师仓库；学生仓库只放面向学生的任务、知识、数据和工具，不混入教师编写说明、评分参考答案或题库。`AGENTS.md`不是跨独立仓库自动生效的全局规则，从其他目录启动任务时先显式读取此处规范。

学生材料使用统计学社区通行术语，并在首次出现时用完整中文和具体例子解释。不自创名称，不用未经解释的专业简称代替教学；语言简化不得改变统计含义或项目要求。具体审校步骤以学生材料编写规范为准。

编辑后核对：项目—知识—自查—评价一致；每课新增能力明确；运行可复现；数据为合成或明确授权；不依赖强制答辩、提示词记录或教师高频手动切换。按规范的六问提供实质审核证据，不仅检查文件存在。

`instructor-guide/archive/`和各仓库`archive/v1-2026-09-05`标签均为历史，只用于追溯，不作为现行模板。修改现行规范需记录原因与受影响课次。保留学生仓库、账号、历史答题记录；题目内容变化使用新的题库ID。发布前检查远端变更，禁止强推覆盖。

## 学生逐课 SUPPORT 模板（现行约定）

每课学生目录可有一份 `SUPPORT.md`，放在同课 `README.md` 与 `LEARN.md` 旁。它只面向学生，不写教师评分答案或题库内容；用途是让基础薄弱、暂时不知道下一步做什么的学生能够独立推进。

每份 SUPPORT 必须按以下顺序组织：
1. **先知道今天要交出什么**：用可检查的文件和结果描述最小出口；
2. **入门跟做**：从一句问题和运行前预计开始，给出仓库根目录、完整命令、要打开的结果文件、至少一次手算或记录核对，以及最小报告路径；
3. **标准完成**：要求实际比较一个合理替代口径，补齐分析单位、数据范围、分子分母、单位、缺失处理、局限和使用建议；
4. **提高与拓展**：提供方法、稳健性、复现、决策等方向，明确不以代码数量或模型数量作为提高标准；
5. **卡住时按顺序排查**：覆盖目录、命令、依赖、旧结果、数字不一致和报告无从下手等常见阻塞；
6. **提交前自检**：核对原始数据未覆盖、结果可复现、`submission.json` 路径与状态、`check` 命令和暂存内容。

写作要求：首次出现统计术语时用通俗中文和本课例子解释；示例数值必须说明只是核对操作，不能代替学生自己的运行结果；入门支持降低操作门槛，但不得删掉本课主任务的统计要求；与 README、LEARN、数据说明和知识自查保持一致。先完善当前发布范围的课次，再按同一模板扩展后续课次。
