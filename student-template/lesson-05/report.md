# 第5课项目报告 · 有限复核名额下的告警策略

> 以下是写作提示，不是已完成报告。请把提示替换为自己的分析、结果和解释；可以调整结构，不要用学习例子的数字代替项目分析。

## 每日复核规则

使用哪些复核前已知的信息？怎样排序、设阈值、处理同分并遵守每日名额？采用哪些前42天资料制定规则，最后确定了什么？若另外估计了故障概率，采用什么方法和数据？

## 前42天的方案比较

至少一个备选方案是什么，为什么值得比较？各方案是否采用相同记录、成本和效果假设？哪些数据同时用于制定规则和评价，结果有哪些限制？

## 后14天的评价结果

保持选择方法不变时，每天选了多少设备？名单可以随当天分数和名额变化，请说明怎样由同一规则得到。报告四格表、假阳性率与假阴性率及其分母、复核成本、遗漏故障和剩余损失。说明故障标记在模拟中保持不变、复核假设只改变损失计算。复核次数与不同设备数是否区分？后期资料是否参与过规则调整？

## 条件变化与使用建议

实际改变了哪个重要条件，名单是否也改变？不同类别设备的结果如何？建议试用、限制使用还是暂不采用？写清可以观察或计算的调整、停止条件。

## 怎样重新计算与记录修改

写出运行命令、需要的软件包、结果文件位置，以及哪些结果支持主要结论。报告中的链接可以写成 `artifacts/文件名`；`submission.json` 中的路径从学生仓库根目录写起。

用到随机模拟时记录随机种子，也就是使随机计算可以重复的初始设置。程序使用相同数据和设置时应得到一致结果。记录实际完成的运行与检查，失败时如实说明；后续修订写清改了什么、为什么。不要求提交AI对话。
